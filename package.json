{
  "name": "libpg-query-node",
  "private": true,
  "description": "Native N-API PostgreSQL query parser",
  "scripts": {
    "build": "npm run build --prefix native",
    "test": "npm run test --prefix native",
    "clean": "npm run clean --prefix native"
  }
}
